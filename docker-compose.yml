version: '3'

services:
  rsyslog:
    image: robbert229/rsyslog
    retart: always
    ports:
      - "514:514"
      - "514:514/udp"
    volumes:
      - docker-rsyslog-storage:/var/log

volumes:
  docker-rsyslog-storage:
